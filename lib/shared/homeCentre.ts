/** homeCentre.ts — the map home, defined once. ⚠️ Never copy these numbers to a second file — stores/mapViewport re-exports this constant, it doesn't re-declare it. */

/** THE map home (45.06°, -76.17°) — where a fresh mount lands when there's nowhere else to go. */
export const MAP_HOME_CENTER: [number, number] = [
	-76.16797958683314, 45.061348227515055,
];
