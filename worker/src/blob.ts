/**
 * ONE SOURCE. This file used to be a byte-identical copy of the engine's
 * `lib/contract/blob.ts` — the Worker and the phone must agree on this geometry,
 * and two copies agree only until someone edits one. Re-exported, not copied.
 */
export * from "../../lib/contract/blob";
