# r2_dev — the copy that matches what tiles-dev.getcache.org is running.

Do not edit here. Code is edited in `../local_dev`; `./deployDev.sh` syncs it
into this folder and deploys, so this folder differing from local_dev means
"not deployed yet" — that gap is the folder's whole message.
