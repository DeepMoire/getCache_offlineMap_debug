# worker-cloud-prod — the copy that matches what tiles-prod.getcache.org is running.

Do not edit here. Code is edited in `../worker-local-dev`; `./deployProduction.sh`
syncs it into this folder (after the confirmation) and deploys. Differing from
worker-local-dev means "not deployed yet" — that gap is the folder's whole message.
